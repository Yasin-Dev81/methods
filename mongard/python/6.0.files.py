"""
with open(PATH, x) as f:
    ...

- x:
    * 'r'
        ~ خواندن فایل
    * 'w'
        ~ نوشتن فایل
    * 'x'
        ~ ایجاد کردن و نوشتن فایل جدید
    * 'a'
        ~ اضافه کردن محتوا به فایل
    * 'r+'
        ~ برای خواندن و نوشتن
"""

with open('my_file.txt', 'r+') as f:
    f.readlines()
    f.read(3)
    x = f.tell() # کاراکتر چندیمم؟
    print(x)

    f.seek(5) # از کاراکتر 5ام شروع میکنه به خوندن
    f.write('test') # نوشتن در فایل
